import { IRequest } from "../../../typings";
import DactylError from "../../../util/error";

export default async function GetDetails(request: IRequest, serverID: string) {
    try {

    } catch (e) {
        throw new DactylError(e);
    }
}